# Copy this file to .env.local and fill in your key to run in "live" mode.
# Without a key, the app runs entirely in deterministic fallback mode —
# the full demo loop still works, just without live Claude calls.
ANTHROPIC_API_KEY=
